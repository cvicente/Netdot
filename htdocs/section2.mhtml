<%doc>

    Section2 is the second-level options menu
    Individual subsections might be later put in their own file
    as they grow

</%doc>
%
%
<%args>
$name => undef
$user => $ENV{REMOTE_USER}
</%args>
%
%
<%init>
my @t;
</%init>
%
%
<div id="content">
%
% if ($name eq "Main" || $name eq ""){
    <h3>Home</h3>
    Objects related to <% $user %>
% } elsif ($name eq "Management"){
    <& section_management.mhtml &>
% }elsif ($name eq "Services"){
    <p />
% }elsif ($name eq "Operations"){
    <& section_operations.mhtml &>
% }elsif ($name eq "Plant"){
    <& section_cable.mhtml &>
% }elsif ($name eq "Generic"){
    <h3>Generic Database Operations</h3>
    <p><b>Note:</b> These functions do not perform any validation
    checking. They are intended to provide a direct access to the
    database.</p>
    <p><b>Do not edit anything here</b> unless you know what you are
    doing.</p>
%
%     map { push @t, $_ unless ($_ =~ /_history/ || $_ eq "Meta") } sort $ui->gettables;
%
    <form action="search_obj.html" method="POST">
        <b>Search</b>:  
        <select name="table">
%       foreach my $t (@t) {
            <option><% $t %></option>
%       }
        </select>
        <input name="submit" value="Go" type="submit">
    </form>

    <form action="edit.html" method="POST">
        <b>Add New</b>:  
        <select name="table">
%       foreach my $t (@t) {
            <option><% $t %></option>
%       }
        </select>
        <input name="submit" value="Go" type="submit">
    </form>

    <form action="browse.html" method="POST">
        <b>Browse</b>:  
        <select name="table">
%       foreach my $t (@t) {
            <option><% $t %></option>
%       }
        </select>
        <input name="submit" value="Go" type="submit">
    </form>
% } elsif ($name eq "Reports"){
    <& section_reports.mhtml &>
% } elsif ($name eq "Help"){
    <p />
% } else{
% }
</div>
